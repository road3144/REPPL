stages:
  - build
  - deploy

variables:
  BACKEND_IMAGE: $DOCKER_HUB_USER/reppl-backend
  FRONTEND_IMAGE: $DOCKER_HUB_USER/reppl-frontend

# ─── Build Backend ────────────────────────────────────────────────────────────
build-backend:
  stage: build
  script:
    - docker login -u $DOCKER_HUB_USER -p $DOCKER_HUB_PASSWORD
    - docker build -t $BACKEND_IMAGE:$CI_COMMIT_SHORT_SHA -t $BACKEND_IMAGE:latest ./backend
    - docker push $BACKEND_IMAGE:$CI_COMMIT_SHORT_SHA
    - docker push $BACKEND_IMAGE:latest
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
    - if: $CI_COMMIT_BRANCH == "dev"

# ─── Build Frontend ───────────────────────────────────────────────────────────
build-frontend:
  stage: build
  script:
    - docker login -u $DOCKER_HUB_USER -p $DOCKER_HUB_PASSWORD
    - docker build -t $FRONTEND_IMAGE:$CI_COMMIT_SHORT_SHA -t $FRONTEND_IMAGE:latest ./frontend
    - docker push $FRONTEND_IMAGE:$CI_COMMIT_SHORT_SHA
    - docker push $FRONTEND_IMAGE:latest
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
    - if: $CI_COMMIT_BRANCH == "dev"

# ─── Deploy ───────────────────────────────────────────────────────────────────
deploy:
  stage: deploy
  script:
    - mkdir -p ~/app/nginx
    - cp docker-compose.yml ~/app/docker-compose.yml
    - cp nginx/nginx.conf ~/app/nginx/nginx.conf
    - |
      printf 'BACKEND_IMAGE=%s\nFRONTEND_IMAGE=%s\nTAG=%s\nEC2_HOST=%s\nAWS_ACCESS_KEY=%s\nAWS_SECRET_KEY=%s\nAWS_REGION=%s\nAWS_S3_BUCKET=%s\n' \
        "$BACKEND_IMAGE" "$FRONTEND_IMAGE" "$CI_COMMIT_SHORT_SHA" "$EC2_HOST" \
        "$AWS_ACCESS_KEY" "$AWS_SECRET_KEY" "$AWS_REGION" "$AWS_S3_BUCKET" \
        > ~/app/.env
    - cd ~/app
    - docker login -u $DOCKER_HUB_USER -p $DOCKER_HUB_PASSWORD
    - docker compose pull
    - docker compose up -d --remove-orphans
    - docker image prune -f
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
  environment:
    name: production
    url: http://$EC2_HOST